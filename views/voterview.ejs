<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="csrf-token" content="<%= csrfToken %>">
    <script src="https://cdn.tailwindcss.com"></script>
    <title><%= title %></title>
    <script>
    </script>
</head>
    </nav>
    <div class=" flex text-gray-800 flex-col  overflow-hidden bg-gray-50 py-6 sm:py-12 px-20">
        <div class="w-full">
            <center>
                <span class="text-4xl font-light "><%= title %></span>
            </center>
            <div>
            <h2 class="px-4 py-2 text-2xl font-light ">Check the below questions and Answer</h2>
                    <div class="pb-2 px-8">
                        <form action="" class="divide-y divide-solid">
                            <ol class="list-decimal mb-5">
                                <% for(let i=0; i<questions.length; i++) { %>
                                    <li class="my-3">
                                        <div class="px-3 py-1">
                                            <h3 class="text-3xl my-3"><%= questions[i].questionname %></h3>
                                            <p class="text-black-800 text-md my-3"><%= questions[i].description %></p>
                                        </div>
                                        <div class="ml-8">
                                            <ol class="list-none">
                                                <% for(let j=0; j<options[i].length; j++) { %>
                                                        <li class="option-item mt-2 cursor-pointer">
                                                            <input type="radio" id="<%= options[i][j].id %>" class="cursor-pointer" name="answer-<%= questions[i].id %>">
                                                            <label class="px-1 my-2 cursor-pointer" for="<%= options[i][j].id %>" class="ml-2 text-lg "><%= options[i][j].optionname %></label>
                                                        </li>
                                                <% } %>
                                            </ol>
                                        </div>
                                    </li>
                                <% } %>
                            </ol>
                            <div class="flex py-4">
                                <button type="submit" class="text-white bg-green-600 hover:bg-green-800 font-medium rounded px-3 py-1">
                                    Submit Response
                                </button>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
</body>
</html>